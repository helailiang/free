[project]
name = "free"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "nuitka>=4.1",
  "pyserial>=3.5",
  "pytest>=9.0.3",
]

[dependency-groups]
# 仅构建 network_test 自动化 exe 时需要：`uv sync --group pyinstaller`
pyinstaller = ["pyinstaller>=6.10.0"]
